# Virtual Assistant Configuration

server:
  host: "0.0.0.0"
  port: 8000
  debug: false

security:
  jwt_secret: "your-secret-key"  # Change this in production
  token_expiration: 3600  # seconds
  max_login_attempts: 3

email:
  smtp_server: "smtp.gmail.com"
  smtp_port: 587
  use_tls: true
  # Add your email credentials securely in production
  sender_email: ""
  sender_password: ""

speech:
  language: "en-US"
  sample_rate: 16000
  chunk_size: 1024
  silence_threshold: 500
  min_phrase_length: 0.5

face_recognition:
  tolerance: 0.6
  model: "hog"  # or "cnn" for GPU support
  number_of_times_to_upsample: 1

database:
  path: "assistant.db"
  backup_interval: 86400  # 24 hours in seconds

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: "assistant.log"
  max_size: 10485760  # 10MB
  backup_count: 5

features:
  enable_voice: true
  enable_face: true
  enable_email: true
  enable_scheduling: true
  enable_media: true
  enable_project_management: true
